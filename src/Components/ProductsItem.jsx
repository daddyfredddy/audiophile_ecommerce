import React from "react";

const ProductsItem = ({
  id,
  slug,
  name,
  image,
  category,
  categoryImage,
  price,
  description,
  features,
}) => {
  return <div>ProductsItem</div>;
};

export default ProductsItem;
